<%= form_with(model: pose) do |form| %>
  <% if pose.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(pose.errors.count, "error") %> prohibited this pose from being saved:</h2>

      <ul>
        <% pose.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.label :image, style: "display: block" %>
    <%= form.text_field :image %>
  </div>

  <div>
    <%= form.label :target_body_area, style: "display: block" %>
    <%= form.text_field :target_body_area %>
  </div>

  <div>
    <%= form.label :seated, style: "display: block" %>
    <%= form.check_box :seated %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
